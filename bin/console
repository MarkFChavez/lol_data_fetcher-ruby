#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "lol_data_fetcher"

# Pre-configured client for easy experimentation
puts "Welcome to LolDataFetcher console!"
puts "Pre-configured variables:"
puts "  client - LolDataFetcher::Client instance (uses latest version)"
puts ""

# Create a client instance for convenience
client = LolDataFetcher::Client.new

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

require "irb"
IRB.start(__FILE__)
